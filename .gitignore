# Generated at Netlify build time
/dist/

# Local Netlify CLI state (netlify-cli serve/dev/link) — machine-local, never part of the site
/.netlify/

# npm dependencies — Netlify installs these from package-lock.json at build time
/node_modules/
