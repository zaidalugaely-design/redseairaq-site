[build]
  command         = "mkdir -p dist && cp index.html dist/ && cp og-default.jpg dist/ && cp -r netlify dist/ && node scripts/stamp-build-version.js && node scripts/generate-og-pages.js"
  functions       = "netlify/functions"
  publish         = "dist"

[functions]
  node_bundler = "esbuild"

[images]
  remote_images = ["https://glhmmrovxyijtzjaldtf\\.supabase\\.co/.*"]

# SPA catch-all — serves index.html for any path with no matching file
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
